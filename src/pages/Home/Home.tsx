import { Stack, Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Stack>
        <Typography variant="h1">Hello World!</Typography>
      </Stack>
    </Container>
  );
};

export default Home;
